# Understand Anything Skill

## Intent

Analyze the local repository structure, map code architectures, or generate visual code graphs using the multi-agent pipeline.

## Actions

When requested to visualize, graph, or analyze the codebase, invoke the underlying tool:

- Execute: `node C:\Users\johng\.gemini\.understand-anything\main.js --auto-update`
